{
  "name": "nats-demo",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "nats:start": "docker compose -f .docker/docker-compose.yaml up -d",
    "nats:stop": "docker compose -f .docker/docker-compose.yaml down",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "nats": "^2.26.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
